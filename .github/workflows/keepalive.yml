name: Render Keep-Alive Cron

on:
  schedule:
    - cron: '*/14 * * * *' # Каждые 14 минут

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Health Check Endpoint
        run: |
          curl -f ${{ secrets.RENDER_APP_URL }}/healthz || exit 1
